{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    // The tests and the tools/ scripts are Node programs, not browser code.
    "types": ["node"],
    "resolveJsonModule": true
  }
}
